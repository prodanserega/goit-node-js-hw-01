const assert = require('assert')
    , knex = require('knex')

    , json = require('./lib/structure')
    , db = require('./lib/db')


exports.initDbConnection = db.initDbConnection

// exports.initRedisConnection = function (connectionConfig) {}

exports.in = {
    database: db.yes,
    // redis: function (target, key) {
    // }
}

exports.notIn = {
    database: db.no,
    // redis: function (target, key) {
    // }
}
exports.structure = json.verifyStructure